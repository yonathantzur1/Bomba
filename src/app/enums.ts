export enum METHOD {
    DEFAULT = "Default Method",
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE"
}

export enum LOG_TYPE {
    LOGIN,
    LOGIN_FAIL,
    REGISTER
}

export enum STATISTICS_RANGE {
    YEARLY,
    WEEKLY
}