export enum METHOD {
    DEFAULT = "Default Method",
    GET = "GET",
    POST = "POST",
    PUT = "PUT",
    DELETE = "DELETE"
}

export const enum LOG_TYPE {
    LOGIN,
    LOGIN_FAIL,
    REGISTER,
    PROJECT_RUN
}

export const enum STATISTICS_RANGE {
    YEARLY,
    WEEKLY
}